const { Role } = require("discord.js");

module.exports = {
    name: 'role',
    description: 'Assign a role in your server',

    args: true,
    	usage: '<user> <role>',
        execute(message, args) {
}
};